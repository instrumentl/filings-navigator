export interface FilerIndexType {
  id: string,
  ein: string,
  name: string,
  line_1: string,
  city: string,
  state: string,
  zipcode: string
}

export interface FilerShowType extends FilerIndexType {
  
}